[build]
  base = "ui/"
  command = "npm ci && npm run build"
  publish = "dist"

# SPA client-side routing — all paths serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
